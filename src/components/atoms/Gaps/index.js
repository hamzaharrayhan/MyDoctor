import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const Gaps = ({height, width}) => {
  return <View style={{height: height, width: width}} />;
};

export default Gaps;

const styles = StyleSheet.create({});
